
%  .bn1 pull letter from cassette on the NEC 2 290.
/cartltr 
	{statusdict begin /letter /manualfeed false store end
	/pgsize 11 inch def} def

%  .bn2 pull manual letter on the NEC 2 290.
/manltr 
	{statusdict begin /manualfeed true store end
	/pgsize 11 inch def} def

%  .bn3 pull legal from cassette on the NEC 2 290.
/cartlgl 
	{statusdict begin /legal /manualfeed false store end
	/pgsize 14 inch def} def

%  .bn4 pull manual legal on the NEC 2 290.
/manlgl 
	{statusdict begin /legal /manualfeed true store end
	/pgsize 14 inch def} def

%  .bn5  pull A4 from cassette on the NEC 2 290.
/carta4 {statusdict begin /a4 /manualfeed false store end
	/pgsize 11.6 inch def} def

%  .bn6 pull manual A4 on the NEC 2 290.
/mana4  
	{statusdict begin /a4 /manualfeed true store end
	/pgsize 11.6 inch def} def

%  .bn7  pull lettersmall from cassette on the NEC 2 290.
/cartls {statusdict begin /lettersmall /manualfeed false store end
	/pgsize 11 inch def} def

%  .bn8 pull manual lettersmall on the NEC 2 290.
/manls  
	{statusdict begin /lettersmall /manualfeed true store end
	/pgsize 11 inch def} def

%  .bn9  pull tabloid from cassette on the NEC 2 290.
/carttb {statusdict begin /tabloid /manualfeed false store end
	/pgsize 17 inch def} def

% .bn10 pull manual tabloid on the NEC 2 290.
/mantb  
	{statusdict begin /tabloid /manualfeed true store end
	/pgsize 17 inch def} def

% .bn11  pull ledger from cassette on the NEC 2 290.
/cartldg {statusdict begin /ledger /manualfeed false store end
	/pgsize 11 inch def} def

% .bn12 pull manual ledger on the NEC 2 290.
/manldg  
	{statusdict begin /ledger /manualfeed true store end
	/pgsize 11 inch def} def

% .bn13  pull statement from cassette on the NEC 2 290.
/cartst {statusdict begin /statement /manualfeed false store end
	/pgsize 8.5 inch def} def

% .bn14 pull manual statement on the NEC 2 290.
/manst  
	{statusdict begin /statement /manualfeed true store end
	/pgsize 8.5 inch def} def

% .bn15  pull executive from cassette on the NEC 2 290.
/cartex {statusdict begin /executive /manualfeed false store end
	/pgsize 10 inch def} def

% .bn16 pull manual executive on the NEC 2 290.
/manex  
	{statusdict begin /executive /manualfeed true store end
	/pgsize 10 inch def} def

% .bn17  pull a3 from cassette on the NEC 2 290.
/carta3 {statusdict begin /a3 /manualfeed false store end
	/pgsize 16.5 inch def} def

% .bn18 pull manual a3 on the NEC 2 290.
/mana3  
	{statusdict begin /a3 /manualfeed true store end
	/pgsize 16.5 inch def} def

% .bn19  pull a4small from cassette on the NEC 2 290.
/carta4s {statusdict begin /a4small /manualfeed false store end
	/pgsize 10.8 inch def} def

% .bn20 pull manual a4small on the NEC 2 290.
/mana4s  
	{statusdict begin /a4small /manualfeed true store end
	/pgsize 10.8 inch def} def

% .bn21  pull a5 from cassette on the NEC 2 290.
/carta5 {statusdict begin /a5 /manualfeed false store end
	/pgsize 8.3 inch def} def

% .bn22 pull manual a5 on the NEC 2 290.
/mana5  
	{statusdict begin /a5 /manualfeed true store end
	/pgsize 8.3 inch def} def

% .bn23 pull b4 from cassette on the NEC 2 290.
/cartb4 {statusdict begin /b4 /manualfeed false store end
	/pgsize 14.3 inch def} def

% .bn24 pull manual b4 on the NEC 2 290.
/manb4  
	{statusdict begin /b4 /manualfeed true store end
	/pgsize 14.3 inch def} def

% .bn25  pull b5 from cassette on the NEC 2 290.
/cartb5 {statusdict begin /b5 /manualfeed false store end
	/pgsize 10.1 inch def} def

% .bn26 pull manual b5 on the NEC 2 290.
/manb5  
	{statusdict begin /b5 /manualfeed true store end
	/pgsize 10.1 inch def} def

% .bn27  pull folio from cassette on the NEC 2 290.
/cartfo {statusdict begin /folio /manualfeed false store end
	/pgsize 13 inch def} def

% .bn28 pull manual folio on the NEC 2 290.
/manfo  
	{statusdict begin /folio /manualfeed true store end
	/pgsize 13 inch def} def

% .bn29  pull quarto from cassette on the NEC 2 290.
/cartqu {statusdict begin /quarto /manualfeed false store end
	/pgsize 10.8 inch def} def

% .bn30 pull manual quarto on the NEC 2 290.
/manqu  
	{statusdict begin /quarto /manualfeed true store end
	/pgsize 10.8 inch def} def

% .bn31  pull 10x17 from cassette on the NEC 2 290.
/cart10x17 {statusdict begin /10x17 /manualfeed false store end
	/pgsize 14 inch def} def

% .bn32 pull manual 10x17 on the NEC 2 290.
/man10x17  
	{statusdict begin /10x17 /manualfeed true store end
	/pgsize 14 inch def} def

% .bn33 pull Monarch envelope on the NEC 2 290.
/envmo  
	{statusdict begin /envelope /envelope-height 279
	/envelopewidth 540 /manualfeed true store end
	/pgsize 10.8 inch def} def

% .bn34 pull #10 envelope on the NEC 2 290.
/env10  
	{statusdict begin /envelope /envelope-height 297
	/envelopewidth 684 /manualfeed true store end
	/pgsize 10.8 inch def} def

% .bn35 pull DL envelope on the NEC 2 290.
/envdl  
	{statusdict begin /envelope /envelope-height 312
	/envelopewidth 624 /manualfeed true store end
	/pgsize 10.8 inch def} def

% .bn36 pull C5 envelope on the NEC 2 290.
/envc5  
	{statusdict begin /envelope /envelope-height 459
	/envelopewidth 649 /manualfeed true store end
	/pgsize 10.8 inch def} def

